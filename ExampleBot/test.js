const colors = require("ultrax").logger;
const {
    red,
    blue
} = colors.colors;
console.log(red + "Hey bro!, it's me " + colors.start_bold + blue + " UltraX" + colors.disable_all)

//colors.error(" OH NOP TOKE STOPLE")